引「../src/应用.yx」为 博客；
引「包:yanxu-web」为 言枢；
引「包:言据」为 言据；
引「标准:测试」为 测试；
引「标准:字节」为 字节；
引「标准:文字」为 文字；
引「标准:JSON」为 JSON；
引「标准:文件」为 文件；

法 含片（原文：文，片：文）：理 则
    归 长度（文字.分割（原文，片））大于 1；
终

令 静态根：文 为「static」；
令 模板根：文 为「templates」；
若 文件.存在（「yanxu-webblog/static/app.css」）则
    置 静态根 为「yanxu-webblog/static」；
    置 模板根 为「yanxu-webblog/templates」；
终
定 所应用 为 博客.创建应用（静态根，模板根）；
定 客户端 为 言枢.测试客户端（所应用）；

定 首页 为 客户端.取（「/」）；
定 首页文：文 为 字节.转文字（首页.正文）；
测试.相等（首页.状态码，200）；
测试.断言（含片（首页文，「言枢小笺」），「首页须显示站点名」）；
测试.断言（含片（首页文，「YANSHU · YANBIAO」），「首页须说明言枢与言标」）；
测试.相等（首页.首部值（「content-security-policy」），「default-src 'self'; style-src 'self'; img-src 'self' data:」）；

定 文章 为 客户端.取（「/posts/hello-yanxu」）；
定 文章文：文 为 字节.转文字（文章.正文）；
测试.断言（含片（文章文，「&lt;标签&gt;」），「言标必须自动转义文章普通文字」）；

定 不见 为 客户端.取（「/posts/missing」）；
测试.相等（不见.状态码，404）；
测试.断言（含片（字节.转文字（不见.正文），「这里没有你寻找的文章」），「404 须使用言标页面」）；

定 JSON响应 为 客户端.取（「/api/posts」）；
定 JSON值：列 为 JSON.解析（字节.转文字（JSON响应.正文））；
测试.相等（长度（JSON值），2）；
测试.相等（JSON值【0】【「id」】，「hello-yanxu」）；

定 言据响应 为 客户端.取（「/api/posts.yj」）；
定 言据值：列 为 言据.解析列（字节.转文字（言据响应.正文））；
测试.相等（长度（言据值），2）；
测试.相等（言据响应.首部值（「content-type」），「application/vnd.yanxu.yanju; charset=utf-8」）；

定 静态响应 为 客户端.取（「/static/app.css」）；
测试.相等（静态响应.状态码，200）；
测试.相等（静态响应.首部值（「content-type」），「text/css; charset=utf-8」）；

定 穿越响应 为 客户端.取（「/static/../言序.toml」）；
测试.相等（穿越响应.状态码，404）；

定 不许 为 客户端.请求（「POST」，「/api/posts」）；
测试.相等（不许.状态码，405）；
测试.相等（不许.首部值（「allow」），「GET」）；

测试.相等（所应用.反向（「文章详情」，{「id」：「hello-yanxu」}），「/posts/hello-yanxu」）；
