引「../src/应用.yx」为 博客；
引「包:yanxu-http」为 HTTP；
引「包:言据」为 言据；
引「标准:测试」为 测试；
引「标准:字节」为 字节；
引「标准:文字」为 文字；
引「标准:JSON」为 JSON；

法 请求（所应用，方法：文，路径：文）则
    归 所应用.处理（HTTP.解析请求头（方法 加「 」加 路径 加「 HTTP/1.1\r\nHost: localhost」））；
终

法 含片（原文：文，片：文）：理 则
    归 长度（文字.分割（原文，片））大于 1；
终

定 所应用 为 博客.创建应用（「yanxu-webblog/static」）；

定 首页 为 请求（所应用，「GET」，「/」）；
定 首页文：文 为 字节.转文字（首页.正文）；
测试.相等（首页.状态码，200）；
测试.断言（含片（首页文，「言序小笺」），「首页须显示站点名」）；
测试.相等（首页.首部值（「content-security-policy」），「default-src 'self'; style-src 'self'; img-src 'self' data:」）；

定 文章 为 请求（所应用，「GET」，「/posts/hello-yanxu」）；
定 文章文：文 为 字节.转文字（文章.正文）；
测试.断言（含片（文章文，「&lt;标签&gt;」），「文章普通文字须自动转义」）；

定 不见 为 请求（所应用，「GET」，「/posts/missing」）；
测试.相等（不见.状态码，404）；

定 JSON响应 为 请求（所应用，「GET」，「/api/posts」）；
定 JSON值：列 为 JSON.解析（字节.转文字（JSON响应.正文））；
测试.相等（长度（JSON值），2）；
测试.相等（JSON值【0】【「id」】，「hello-yanxu」）；

定 言据响应 为 请求（所应用，「GET」，「/api/posts.yj」）；
定 言据值：列 为 言据.解析列（字节.转文字（言据响应.正文））；
测试.相等（长度（言据值），2）；
测试.相等（言据响应.首部值（「content-type」），「application/vnd.yanxu.yanju; charset=utf-8」）；

定 静态响应 为 请求（所应用，「GET」，「/static/app.css」）；
测试.相等（静态响应.状态码，200）；
测试.相等（静态响应.首部值（「content-type」），「text/css; charset=utf-8」）；

定 穿越响应 为 请求（所应用，「GET」，「/static/../言序.toml」）；
测试.相等（穿越响应.状态码，404）；

定 不许 为 请求（所应用，「POST」，「/api/posts」）；
测试.相等（不许.状态码，405）；
