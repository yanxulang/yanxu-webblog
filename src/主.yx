引「包:yanxu-web」为 言枢；
引「标准:环境」为 环境；
引「标准:文字」为 文字；
引「应用.yx」为 博客；

法 项目路径（名称：文）：文 则
    若 文字.终于（环境.当前目录（），「yanxu-webblog」）则 归 名称；终
    归「yanxu-webblog/」加 名称；
终

令 地址：文 为「127.0.0.1:8787」；
定 配置地址：文? 为 环境.读取（「YANXU_WEBBLOG_ADDR」）；
若 配置地址 是 文 则
    若 配置地址 不等于「」则 置 地址 为 配置地址；终
终

定 所应用 为 博客.创建应用（项目路径（「static」），项目路径（「templates」））；
定 服务器 为 言枢.服务器（所应用，地址）；
言「言枢小笺：http://」加 地址；

若 包含（环境.参数（），「--once」）则
    服务器.运行若干（1）；
否则
    服务器.运行（）；
终
