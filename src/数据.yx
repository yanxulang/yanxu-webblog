/// 博客示例数据；每次返回新容器，避免处理器之间共享可变列。

公 法 全部文章（）：列 则
    归【
        {
            「id」：「hello-yanxu」，
            「title」：「以言枢写第一个 Web 项目」，
            「summary」：「从配置与命名路由开始，再把页面交给言标安全渲染。」，
            「body」：「普通文字里的 <标签> 会被言标自动转义；只有显式可信 HTML 值才能绕过边界。」，
            「date」：「2026-07-14」
        }，
        {
            「id」：「three-layers」，
            「title」：「言枢与言标，一条清晰应用链」，
            「summary」：「言标组织页面，言枢编排应用，HTML 与 HTTP 基础层各守边界。」，
            「body」：「言标负责模板与自动转义，言枢负责配置、路由和中间件，底层库负责节点安全与协议字节。」，
            「date」：「2026-07-15」
        }
    】；
终

公 法 查文章（编号：文）：典? 则
    逐 文章：典 于 全部文章（）则
        若 文章【「id」】等于 编号 则 归 文章；终
    终
    归 空；
终
